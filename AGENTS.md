# 项目开发约定

每次新增功能、Bug 修复及其他代码变更完成后，必须自动完成以下交付流程，无需再次询问用户是否构建、安装、启动、提交或推送：

**强制执行顺序：测试通过并生成本次构建产物 → 杀死所有 Wnip 进程并确认无残留 → 完整替换 `/Applications/Wnip.app` → 从该路径重新启动并验证 → 提交并合并到 `main` → 推送到 `origin/main` 并核对远端提交。测试通过后必须继续完成全部步骤，不得跳过或仅告知用户手动执行。**

1. 执行项目适用的测试及构建或打包流程，生成本次变更的应用产物。
2. 终止所有 Wnip 进程，并确认没有残留进程。
3. 使用本次构建产物完整替换 `/Applications/Wnip.app`。
4. 使用 `open /Applications/Wnip.app` 重新启动应用，核对实际运行路径并完成基本运行验证。
5. 提交本次任务的代码变更，按下述约定合并到 `main`，并自动推送到远端。

如果测试、构建、打包、安装、启动、运行验证或推送中的任一步骤失败，应继续排查、修复并重新执行，直至成功；不得仅以源码修改完成或测试通过作为任务完成标准。遇到必须由用户处理的权限或外部服务阻塞时，必须明确报告失败步骤、已完成事项和解阻条件，不得宣称交付完成。

## 分支合并约定

- 新功能开发、Bug 修复及上述全部验证完成后，如果本次开发位于非 `main` 分支，必须自动将该分支合并回 `main` 分支，不得仅停留在功能分支上。
- 合并结果如包含未验证的代码变化，必须重新构建、安装、启动并验证最终 `main` 版本。

## 代码提交与远端推送

- 每次功能或 Bug 修复交付必须自动创建提交，并执行普通推送，将最终 `main` 推送至 `origin/main`；不得仅保留在本地提交或未提交工作区。
- 仅提交本次任务相关变更，保留用户原有及其他任务的未提交修改，不得为提交或合并而丢弃、覆盖这些改动。
- 推送后必须核对远端 `main` 的提交 SHA 与本地最终提交一致，确认本次代码已到达远端。
- 最终回复必须说明测试结果、安装与启动验证结果，以及提交 SHA 和远端推送结果。

## macOS 应用安装与运行验证

- Wnip 的唯一安装位置必须是 `/Applications/Wnip.app`；不得安装到 `~/Applications`、DerivedData、工作树或其他路径。
- 每次安装前必须先终止所有 Wnip 进程，并确认没有残留进程；不得在 Wnip 运行时覆盖应用包。
- 必须用本次构建的产物完整替换 `/Applications/Wnip.app`，不得保留或启动旧安装副本。
- 安装后必须使用 `open /Applications/Wnip.app` 从固定路径启动；不得使用 `open -a Wnip`，不得从 Xcode、DerivedData 或构建输出目录直接启动用于手工验证。
- 启动后必须核对实际运行的可执行文件路径为 `/Applications/Wnip.app/Contents/MacOS/Wnip`，然后再执行截图、系统权限和 UI 等基本运行验证。
- 如果发现 `/Applications/Wnip.app` 以外的已安装 Wnip 副本，必须先将副本移入废纸篓或取消系统注册，再继续验证；DerivedData 中的构建产物可保留，但不得把它们当作已安装应用启动。
